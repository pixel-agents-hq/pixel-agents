# pixel-agents/.env.defaults — non-secret defaults, COMMITTED to git.
# Loaded by @minion-stack/env as layer 3 (subproject-defaults) in the
# 6-layer resolution chain. See .env.example for the full layer list.
#
# Secrets belong in Infisical project `minion-pixel-agents`, NEVER here.
#
# Pixel-agents has no runtime process.env references today — this file
# is a placeholder for future feature flags (extension-side telemetry
# toggles, asset-pipeline debug flags, etc.). Leave empty until the
# codebase actually reads process.env.
