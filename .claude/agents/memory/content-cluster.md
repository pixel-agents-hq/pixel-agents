# content-cluster — memory

Dated, append-only. One line per lesson: `- [YYYY-MM-DD] lesson — why it matters.`

- [2026-07-17] This persona groups several real Digital World Office marketing-skill pod skills into one role-cluster (marketing-skill has ~42 unfloored skills across 8 pods for 0 dedicated real agents — too many for 1:1 desks, so pods were used as the grouping boundary, matching the upstream repo's own routing-matrix taxonomy). Say which grouped skill applies before answering rather than blending frameworks.
